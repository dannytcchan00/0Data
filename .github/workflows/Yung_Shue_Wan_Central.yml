name: Auto Download HKKF Yung_Shue_Wan_Central CSV

on:
  schedule:
    - cron: '0 1 * * *'  # 每日UTC 1點 (香港9點)
  workflow_dispatch:

permissions:
  contents: write  # 關鍵：授予寫入權限

jobs:
  download_and_commit:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Setup directories and download CSV
        run: |
          mkdir -p data/travel/HKKF/Yung_Shue_Wan_Central/
          curl -L -o data/travel/HKKF/Yung_Shue_Wan_Central/data.csv \
            "https://www.hkkfeta.com/opendata/time_table/2/inbound"
          
          echo "✅ CSV size: $(wc -c < data/travel/HKKF/Yung_Shue_Wan_Central/data.csv) bytes"

      - name: Commit and push if changed
        uses: EndBug/add-and-commit@v9
        with:
          add: 'data/travel/HKKF/Yung_Shue_Wan_Central/data.csv'
          message: '🤖 Auto Download HKKF Yung_Shue_Wan_Central CSV ({{ github.run_number }})'
          default_author: github_actions
