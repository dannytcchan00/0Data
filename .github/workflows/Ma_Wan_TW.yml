name: Auto Download Ma Wan TW CSV

on:
  schedule:
    - cron: '0 1 * * *'  # 每日UTC 1點 (香港9點)
  workflow_dispatch:

permissions:
  contents: write  # 關鍵：授予寫入權限

jobs:
  download_and_commit:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Setup directories and download CSV
        run: |
          mkdir -p data/travel/Ma_Wan/TW
          curl -L -o data/travel/Ma_Wan/TW/data.csv \
            "https://www.td.gov.hk/datagovhk_td/ferry-tt-ft/resources/en/ferry_mawan_tw_timetable_eng.csv"
          
          echo "✅ CSV size: $(wc -c < data/travel/Ma_Wan/TW/data.csv) bytes"

      - name: Commit and push if changed
        uses: EndBug/add-and-commit@v9
        with:
          add: 'data/travel/Ma_Wan/TW/data.csv'
          message: '🤖 Auto Download Ma Wan TW CSV ({{ github.run_number }})'
          default_author: github_actions
